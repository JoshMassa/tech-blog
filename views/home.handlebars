{{#each blogPosts as |blogPost|}}
<div class="row mb-4">
    <div class="col-md-5">
        <h2>
            <a href="/blogPost/{{this.id}}">{{this.title}}</a>
        </h2>
    </div>
</div>
{{/each}}