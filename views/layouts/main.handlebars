<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Tech Blog</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <div>
        <header>
            <nav id="navbar">
                <ul>
                    <li><a class="navbarEl" href="/">Home</a></li>
                    <li><a class="navbarEl" href="/dashboard">Dashboard</a></li>
                    {{#if loggedIn}}
                        <li><a class="navbarEl" href="/logout">Logout</a></li>
                    {{else}}
                        <li><a class="navbarEl" href="/login">Login</a></li>
                    {{/if}}
                    <p id="title">{{this.title}}</p>
                </ul>
            </nav>
        </header>
    </div>
    {{{body}}}
</body>
</html>